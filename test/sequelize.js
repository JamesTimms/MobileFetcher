var assert = require('assert');
var sequelizeInit = require('../app/models/sequelizeInit.js');

describe('testing the database ORM', function () {
    describe('testing table creation', function () {
        it('should create and/or add a table from model', function (done) {
            // sequelizeInit(done);
            done();//Test unfinished. Will complete once sqlite local database it built.
        });
    });
});
